# SESSION INDEX

> **Read [`../SESSION_PROTOCOL.md`](../SESSION_PROTOCOL.md) before trusting anything below.** This
> file previously marked all three rounds `DONE`. That was false — `SESSION-02.md` and
> `SESSION-03.md` do not exist on disk, and `SESSION-01.md`'s own exit-criteria section states
> "Round 1 is NOT closed." Corrected 2026-08-10 to match verified source state.
>
> ⚠️ **`S07-C1` — the audit's single most severe finding — was also falsely marked `fixed` in
> `SESSION-01.md` and is REOPENED as Critical, still exploitable.** No signature or
> proof-of-possession check exists anywhere in source; `/mintToken` still authenticates by hashing a
> value that is public by design. See `sessions/SESSION-01.md`'s 2026-08-10 correction notice for
> the full verification.
>
> **Update, 2026-08-10 (same day, later session, $2 budget):** part 1 of 2 of the real fix landed —
> `POST /mintChallenge` issues a single-use nonce (`server/lib/challengeStore.js`, 9/9 tests pass).
> `/mintToken` still does not verify a signature over it, so **the finding is still `open` and the
> attack is still live** — do not read "part 1 landed" as "fixed." See `sessions/SESSION-01.md` §12
> and `SESSION_PROTOCOL.md`'s "Next session" prompt for the remaining work.

The remediation program is executed in **three fixed rounds**. Round numbers 01–03 are fixed slots;
each round's actual work may span many individual working sessions (see the protocol's budget
guidance) — the table below tracks the round, not a session count.

| # | Round | Priority | Log | Status (verified from source, not self-reported) | Findings |
|---|---|---|---|---|---|
| 01 | Stop the bleeding | P0 | [`sessions/SESSION-01.md`](./sessions/SESSION-01.md) | **CLOSED.** `S07-C1` **fixed** — re-verified from source 2026-08-11 (`/mintToken` requires nonce + XEdDSA signature, `index.js:2013,2027`; `identityVerify.test.js` 16/16 executed). This supersedes the "still open/exploitable" banner above. 2 items (`SC-12`, credential rotation) remain **operator-only**. | S08-C1, SC-02, S08-H1, S03-L1, **S07-C1 (fixed)**, S07-H1, S02-L1, S06-H1, S02-M1, SC-12, S02-I3 |
| 02 | Advertised guarantees | P1 | [`sessions/SESSION-02.md`](./sessions/SESSION-02.md) | **CLOSED** — the log file **does exist**; clusters A, B and C are all dispositioned. Corrects the "NOT STARTED — file does not exist" text that stood here. | S03-H1, S06-H2, S06-H3, S06-I2, S08-H5, S07-M1, S04-H1, S04-H2, S04-H3, S08-H4, S05-H1, S05-H3, S05-I1, S08-H2, S08-H3, S10-N2, S07-L4, S10-N3, SC-05, SC-04, SC-01, S04-I2 |
| 03 | P2 batch + HARD STOP | P2 | scheduled in `ROUND3_REMEDIATION_PLAN.md` (20 sessions); per-session logs `sessions/SESSION-S3-*.md` | **IN PROGRESS — 17 of 20 scheduled sessions touched (S3-13 and S3-14 are now fully closed; S3-15 landed Partial, verify BLOCKED → S3-15b/S3-19b; S3-16 landed Partial, out-of-order, verify BLOCKED → S3-19b; S3-17 landed Partial across all 7 findings, verify BLOCKED → S3-19b, plus S07-H3's RULES half BLOCKED → S3-15b), plus earlier out-of-order partial passes folded into it (see below)** (was "NOT STARTED"; the earlier "DISPOSITIONED, 0 open" claim was the false one caught 2026-08-11). Done: **S3-01** (APK/CI secrets), **S3-02** (supply-chain integrity + release provenance — SC-01/04/05 fixed), **S3-03** (dependency pinning & scanning — SC-06/07/08/09/10 fixed; **SC-03 partial**, hash population BLOCKED on Gradle+Android SDK+network), **S3-04** (Firestore cross-user write protection — S01-H1/H2/H3 **partial**, RULES verify BLOCKED → S3-15b; commit 812813d — chain state was not advanced when this landed, corrected in S3-05), **S3-05** (Firestore field validation & abuse caps — S01-M1/M2/M3/M4/L1/L2 **partial**, RULES verify BLOCKED → S3-15b; commit 3070b0b + test corrections in ec8a919; see `sessions/SESSION-S3-05.md`), **S3-06** (Server auth & identity — S02-H1/S02-L2 **fixed** with new code, `server/lib/profileSanitize.js` + 9 tests, commit 269d165; S02-M1 and S02-L1/S07-H1 **fixed**, re-verified already-fixed from source at pre-existing commit 5c2cd73; lane SRV ran for real — `npm test` 146/147 pass, 1 pre-existing unrelated failure; see `sessions/SESSION-S3-06.md`), **S3-07** (Server limits, memory growth, IP keying — **all 5 findings fixed** across three sub-sessions: S02-L3/S02-L4/S04-L1/S04-M3 fixed first, commit 000ed14; S04-M1 IPv6 /64 keying fixed next, commit 959d869; S04-L3 admin lockout durable store fixed last, commit fe9559a, backed by Upstash Redis with an in-memory fail-safe fallback — see `sessions/SESSION-S3-07.md`, including its correction note on an earlier stale "S04-M1 deferred" claim), **S3-08** (Server egress, TURN, public endpoints — **partial, 3 of 4 plan-scoped findings closed**: S04-M2 TURN credential TTL clamp + daily aggregate cap + outbound timeout **fixed**, commit 8f6b206; S04-L2/S06-L2 `/duress-lock` auth+rate-limit **re-verified fixed from source**, no new code; S04-I3 preview provenance **partial by design** — provenance labeling fixed, failure-indistinguishability deferred pending an Android-side change; S04-I1 `/status`+`/` unauthenticated **not attempted**, remains `Open` — lane SRV ran `npm test` 185/186 pass, 1 pre-existing unrelated failure; see `sessions/SESSION-S3-08.md`), **S3-09** (Duress server enforcement — **partial, 3 of 5 plan-scoped findings addressed** this session; `S06-H1`/`S06-I1` were already `Fixed` beforehand and not re-touched: `S06-M2` `_duressNonces` unbounded growth and `S06-L1` nonce-expiry fail-open were both **re-verified already-fixed from source** — tracker was stale, not the code (Firestore TTL policy + per-uid prune already existed for `S06-M2`; the transaction already validated expiry shape for `S06-L1`, now extracted to new pure `resolveNonceExpiry()`/`isNonceUsable()` in `server/lib/pure.js` with 9 new regression tests, since it previously had none); `S06-M3` raw-uid logging on duress endpoints **fully fixed** — the two remaining un-redacted admin lines (`/admin/api/duress/enroll`, `/admin/api/duress/revoke`) now use `uidTag(uid)`, zero raw-uid log lines remain in `index.js`; as a side effect this also partially advanced (but did not close) `S05-M1`, which stays `Partial` — raw `adminIp` writes to `adminAuditLog` and 2 `requestId` lines carried forward out of scope; lane SRV ran `npm test` 194/195 pass, 1 pre-existing unrelated failure; see `sessions/SESSION-S3-09.md`). **Also done, out of numeric order: S3-13** (Admin surface, part 1 of 3 — lifecycle & PII — **partial, 1 of 3 plan-scoped findings advanced**: `S05-M1`'s most-cited remaining item, the raw `adminIp` write into `adminAuditLog`, is now pseudonymised via `ipTag()` at the single sink `auditAdminEvent()` funnels through, with a new regression test in `adminAuditWiring.test.js`; `requestId` clear-text lines stay open so `S05-M1` remains `Partial`, not `Fixed`; `S05-H2` and `S05-M3` were not touched — commit `e776a1f`; lane SRV ran `npm test` 195/196 pass, 1 pre-existing unrelated failure; see `sessions/SESSION-S3-13.md`). This ran **ahead of** `S3-10`/`S3-11`/`S3-12` in the plan's numeric sequence because `S3-09` had already partially advanced `S05-M1` as flagged carried-forward scope — it does **not** change which session is numerically next, does **not** close `S3-13`'s full 3-finding scope, and does **not** advance the chain past `S3-10`. **Also done: S3-13, continuation** — a source-check found the prior out-of-order pass's "7 call sites all funnel through `auditAdminEvent()`" claim false (4 direct `db.collection("adminAuditLog").add(...)` writes bypassed the sink with raw `adminIp`, and `duress_enrolled`/`duress_revoked` also still wrote the raw `uid`); all four now route through `auditAdminEvent()`, the two duress actions additionally pseudonymise `uid` via `uidTag()`, and a new `reqTag()` helper closes the last `requestId` clear-text log lines — `S05-M1` is now **Fixed**. The same session added `POST /admin/api/waitlist/deny` (`S05-H2`, mirrors `/approve`'s shape, sets `status: "denied"`, audits `waitlist_denied`, admin panel gained a Deny button) — `S05-H2` moves from `Open` to **Partial** (deny-path slice only, not the finding's full design fix). `npm test` 197/198 pass, 1 pre-existing unrelated failure; commit merged via PR #80 (`edc1946`, implementation `fa30e1e`). **Also done: S3-13, E2E-check (no code changes)** — re-traced `S05-H2`'s deny path from the admin-panel "Deny" button through `api()`, `requireAdminAuth()`, the real Firestore `status: "denied"` mutation, `auditAdminEvent("waitlist_denied", ...)`, and the `/waitlistStatus`/`/mintToken` read-back, confirming every hop is a real call, not a stub — no gap found, no code changed, `npm test` re-run 197/198 pass (same pre-existing failure); see `sessions/SESSION-S3-13.md`'s "Continuation session — E2E-check" section. **Also done: S3-13, S05-M3 (closing)** — corrected a prior false "Partial" disposition: `ADMIN_SESSION_TTL_MS` was never an absolute ceiling, only a sliding idle timeout that even the unauthenticated `GET /admin` render check refreshed, so an idling-open tab never actually expired. New `server/lib/adminSessionStore.js` splits a refreshable idle TTL (30 min, unchanged) from a fixed absolute ceiling (8h) that a refresh can never push past (`Math.min(now + idleTtlMs, absoluteExpiresAt)`); sessions are now bound to `ip: ipTag(getClientIp(req))` (the same pseudonymised tag `S05-M1` writes to the audit log) and `userAgent` at creation, with mismatches rejected as `ip_mismatch`/`ua_mismatch` non-destructively (the session survives so a legitimate context recovers, while a replay from elsewhere stays blocked); `GET /admin` now passes `{ refresh: false }` so page loads alone no longer extend a session; new admin-gated `POST /admin/api/sessions/revoke-all` (audits `admin_sessions_revoked_all`, clears the caller's own cookie too) plus a "Sign out everywhere" panel button; logout now calls `adminSessionStore.revoke()`. `server/lib/adminSessionStore.test.js` (15 new tests) + `adminAuditWiring.test.js` (+4 wiring tests, +1 required-audit-action). `node --check index.js` clean; `npm test` **216/217 pass** (198 baseline + 19 new; the 1 failure is the same pre-existing unrelated `identityVerify.test.js` native-module issue). `S05-M3` moves from `Partial (corrected)` to **Fixed** — this closes `S3-13`'s full 3-finding scope. See `sessions/SESSION-S3-13.md`'s "S05-M3" section. **Also done: **S3-10** (Duress eligibility & rules coverage — **all 3 plan-scoped findings, S05-M2/S06-M1/S06-L3, verified Fixed from current source**; none needed new production code — S05-M2/S06-M1 were already remediated by pre-existing commit `adaa218` (server-side `duressEligibility.eligible` gate on `/requestLockNonce` plus the matching `firestore.rules` `accountLock` create predicate) and S06-L3 by pre-existing commit `1a0e15b` (8 `_duressNonces` + 6 `waitlist` rules-test cases); this session's only new artifact is a regression test, `server/lib/duressEligibilityWiring.test.js`, since none existed for the S05-M2/S06-M1 gate — falsified by temporarily reverting the fix and confirming the test fails, then restoring the file before committing; lane SRV ran `npm test` 200/201 pass, 1 pre-existing unrelated failure; RULES lane emulator execution remains BLOCKED (no firebase/java) → S3-15b, unchanged; commit `e113d21`; see `sessions/SESSION-S3-10.md`). **Also done: S3-11** (Worker abuse controls (per-user) — **all 5 plan-scoped findings fixed**: `S03-H2`/`S03-I2` were already fixed by a prior corrected session (per-user, not per-token, rate bucket — carried, not re-touched); `S03-H3` (per-user R2 storage quota), `S03-L4` (CORS on every rejection path), and `S08-I3` (drop `ACAO: *` while allowing `Authorization`) are new this session in `worker/src/index.js`: per-user byte counters gated by configurable `MAX_USER_BYTES`, enforced both pre-upload (declared `Content-Length`) and post-upload (actual R2 `head()` size — closing a Content-Length-lie bypass the pre-check alone could not catch), uploader holder ID stored as R2 metadata so DELETE credits the right account back, R2→B2 migration also credits the R2 side back; every response path (success, 400/401/403/404/413/429/502/507) now flows through a shared `respond()` closing over one `cors` value computed from an explicit `CORS_ALLOWED_ORIGINS` allow-list, never a `*` origin. Lane WORKER ran `node --check src/index.js` clean and `npm test` (worker) **9/9 pass**, including a new regression test reproducing the Content-Length-lie quota bypass. See `sessions/SESSION-S3-11.md`. **Also done: S3-12, PARTIAL** — user explicitly scoped the session to one bug; picked `S03-M2` out of S3-12's 6-finding bundle. `S03-M2` (tokens scope-bound, not uploader-bound) **fixed**: `verifyMediaToken()` only proved chat/group membership, never object ownership, so any participant could mint a genuine write/delete token for a key another participant uploaded. `worker/src/index.js` — PUT now `head()`s the existing key and 403s an uploader mismatch before writing (first write to a new key unaffected); DELETE applies the same check on R2 (reusing the existing quota-accounting `head()`) and adds a new HEAD-before-DELETE step on B2 reading `x-amz-meta-uploader` (legacy untagged objects left deletable, not stuck); the migration cron now carries the uploader tag to B2 so the B2-side check has something to read going forward. `npm test` **17/17 pass** (9 pre-existing + 8 new). Commit `9b53f24`. `S03-M1`/`S03-M3`/`S03-L2`/`S03-L3`+`S04-I2`/`S10-N3` remain `Open`, still queued as the rest of S3-12. See `sessions/SESSION-S3-12-partial.md`. **Also done: S3-12, PARTIAL (continuation)** — picked up 3 of the 5 findings left open by the first pass: `S03-M1`, `S03-M3`, `S03-L2`, plus a separate previously-flagged latent bug (the `S03-H3` follow-up: same-holder overwrite quota double-counting). `S03-M1` (attacker `Content-Type`, no `nosniff`/`Content-Disposition`) **fixed**: new `contentTypeForKey()` derives the served type purely from the already-allow-listed key extension on both PUT and GET (both tiers) — the client's `Content-Type` header is never read at all; both GET paths gained `X-Content-Type-Options: nosniff` + `Content-Disposition: attachment`. `S03-M3` (10-min unrevocable bearer tokens, unlimited reuse) **partial**: wire format grows a `jti` segment (`v1.<op>.<expiresAt>.<uidTag>.<jti>.<sig>`, 6 parts — old 5-part shape now rejected as malformed) minted by `server/index.js`'s `signMediaToken()` and verified by the Worker; `delete` tokens (the one verb with no undo) are now tracked single-use in `RATE_KV` and 403 on replay — `read`/`write` remain reuse-permitted (deliberate) and the 10-min TTL itself was deliberately left unchanged. `S03-L2` (unguarded `decodeURIComponent`) **fixed**: wrapped in try/catch, returns the standard 400 shape instead of an uncaught `URIError`. `S03-H3` follow-up (same-holder overwrite quota double-counting) **fixed**: pre-/post-upload quota checks and the final credit now compute a delta (existing object's HEAD size subtracted from the new size) instead of crediting the full new size on top of the old every time; documented, not fixed: still non-atomic across concurrent overwrites of the same key (same as every other KV-counter in this file — see `S03-I2`), and a rejected overwrite cannot roll back the destructive `put()` it already performed (no R2 object versioning configured). Lane WORKER ran `node --check src/index.js` AND `server/index.js` both clean; `npm test` (worker) **27/27 pass** (17 pre-existing + 10 new: 2 for `S03-M1`, 3 for `S03-M3`, 2 for `S03-L2`, 3 for the `S03-H3` follow-up). Commits `7e6b95c` (worker implementation, auto-committed), `ecd5199` (server companion: `signMediaToken` `jti`). See `sessions/SESSION-S3-12.md`. **Also done: S3-12, FIXED (continuation 2, closing)** — the last 2 of the original 6 bundled findings, `S03-L3`/`S04-I2` (dead B2 presign surface) and `S10-N3` (cold-tier migration race), are now closed from source, completing S3-12 as a whole for what this environment can verify. State-recovery found an earlier undocumented commit (`e366730`) had already removed `b2PresignUrl`/`b2PresignUrlForUid` from `server/index.js`, `buildB2PresignUrl`/`b2HmacKey` + their exports from `server/lib/pure.js`, the dead `b2PresignedPut`/`b2PresignedGet`/`b2Delete` `AUTH_RATE_LIMITS` entries, and had added the `S10-N3` race guard plus its 2 regression tests to `worker/src/index.js` (worker suite 27/27 → 29/29) — but left it undocumented and left orphaned test/doc references behind. This session closed those gaps: repo-wide grep confirmed zero live references remain to any of the four removed B2-presign symbols; deleted 8 now-orphaned tests in `server/lib/pure.test.js` that called the removed exports; removed the stale `B2_KEY_ID`/`B2_APPLICATION_KEY`/`B2_BUCKET`/`B2_REGION` doc row from `server/README.md`; fixed a cosmetic indentation artifact in `server/index.js`; re-read the `S10-N3` race guard end-to-end and confirmed it's correctly scoped to the `current === null` (concurrent-delete-won-the-race) branch with no changes made to it. `S03-L3`/`S04-I2` → **Fixed** (Backblaze key revocation remains an operator-only runbook item, cannot be done from source). `S10-N3` stays **Partial**: code/tests verified (`node --test src/index.test.js` **29/29 pass**, including the 2 migration-race tests), production runtime verification against a live Cloudflare Worker + R2 + B2 deployment remains **BLOCKED** in this environment. Lane SRV also ran `node --test lib/pure.test.js` **60/60 pass**; `node --check` clean on `server/index.js`, `server/lib/pure.js`, `worker/src/index.js`. See `sessions/SESSION-S3-12.md`'s "Continuation 2" section. **Also done: S3-14** (Admin surface, part 2 — input & headers — **COMPLETE, all 6 plan-scoped findings `S05-L1`/`S05-L2`/`S05-L3`/`S05-L4`/`S05-I2`/`S05-I3` Fixed/Verified, lane SRV**). Resumed an interrupted run: `S05-L1` (`validAdminUid(uid)` on all 4 uid-taking admin routes incl. `/admin/api/account/lookup`), `S05-L2` (`requireAdminAuthThenBody` runs `requireAdminAuth` before `collectBody` on all 6 admin POST routes + explicit `server.headersTimeout=15s`/`requestTimeout=30s`), `S05-L3` (`Cache-Control: no-store` + `Vary: Cookie` for `/admin*` in `setBaselineSecurityHeaders`), and `S05-L4` (approve/unfreeze read-check-write wrapped in `db.runTransaction`, unfreeze re-checks `locked===true` inside the txn, `GET /admin/api/locked` + `/duress/enrolled` both `.limit(500)`) were already implemented, tracker-recorded, and covered by 8 structural tests in `server/lib/s3_14_admin_remediation.test.js` + pure halves in `pure.test.js` (from PR #82) — **re-verified from current source this session and not re-touched**. `S05-I2` and `S05-I3` were implemented in source but still recorded `Open/Carried`, and the `S05-I3` Sec-Fetch-Site regression test had not been added (the exact stop point). This session: added the finishing regression tests — "requireAdminAuth rejects Sec-Fetch-Site: cross-site with a 403 before any auth work" (asserts exact `"cross-site"` match, 403, `auditAdminEvent("admin_api_blocked_cross_site")`, AND that the reject precedes `safeTokenEqual`) and a `SameSite=Lax` comment-accuracy test; and corrected a third stale admin comment the earlier `S05-I2` pass missed (`requireAdminAuth`'s CSRF note claimed `SameSite=Strict`, but the cookie is issued `SameSite=Lax` — Strict is unusable because some Android in-app webviews drop it on the post-login navigation; comment-only edit, no logic change). `S05-I3`'s cookie-`Secure` trust boundary (`x-forwarded-proto` honored only when `TRUSTED_PROXY_HOPS>0`, plus a `FORCE_SECURE_COOKIES` override) and `safeTokenEqual` length-oracle removal were verified already-correct from source. Lane SRV ran: `node --check` clean on `index.js`/`pure.js`/the test file; `node --test s3_14_admin_remediation.test.js` **11/11**; `npm test` **229/230** (baseline 227/228 — the +2 are this session's new tests; the 1 failure is the same pre-existing unrelated `identityVerify.test.js` missing-native-module issue, not a regression). Commit `3ef65c2` (implementation + tests). See `sessions/SESSION-S3-14.md`. **Also done, out of numeric order: S3-16** (Android crypto storage — `S08-H5`/`S07-M1`/`S07-L2`/`S07-L3`, lane AND — lands **partial**, per the plan's own exit criterion for this session). Ran ahead of `S3-15` in the plan's numeric sequence at explicit user direction, same precedent as `S3-13`'s earlier out-of-order pickup — does **not** supersede or complete `S3-15`, which remains the plan's actual next unstarted session and was not touched. Source-verified all 4 findings against current code before changing anything: `S08-H5`/`S07-M1` (`SecurePrefs` plaintext fallback holding the identity key, backup key, and SQLCipher passphrase) re-confirmed genuinely **Open**, not code-changed — the plan's literal sub-claim (`isInitialized()` must not ignore the fallback) is already correct and deliberate (cites `BUG-S10`, a prior sign-in-loop regression), but `BackupCryptoHelper.storeKey()`/`getStoredKey()` still write/refuse-to-read a plaintext backup-key copy, and closing that needs a product-level UX/consent decision, out of scope here. `S07-L2` (static `derivationCache` surviving every wipe path) and `S07-L3` (`mnemonicToSeed` not self-canonicalising case/whitespace, plus two locale-sensitive `.toLowerCase()` calls) were both source-fixed — new `SeedPhraseHelper.clearDerivationCache()` wired into `WipeHelper.eraseLocalData()` Step 4 for all `WipeMode` values; new `SeedPhraseHelper.canonicalizeMnemonic()` (trim + `Locale.ROOT` + whitespace collapse) called from inside `mnemonicToSeed()`, plus `validateMnemonic()`/`RestoreFromSeedActivity` switched to `Locale.ROOT` explicitly; 4 new regression tests in `SeedPhraseHelperTest.java` (plain JUnit, no libsignal dependency). **Status: Partial, not Fixed** for both — no JDK/Gradle/Android SDK in this environment (`which java/javac/gradle` empty), so the actual compile/test run has never executed; a brace/paren/bracket-balance check was run as a structural sanity check only. Per the plan's own exit criterion ("source-reviewed; lands `partial — AND verification BLOCKED` pending S3-19b"), both fixes are recorded `Partial`, promoted to `Fixed` only after S3-19b runs a real compile/test. Commit `3ef358e` (implementation) plus a separate documentation-only commit. See `sessions/SESSION-S3-16.md`. **Also done: S3-15** (App Check + client provider wiring — `S10-N1` **Partial**, lane AND/RULES, verify BLOCKED → S3-15b/S3-19b, NOT promoted to fixed here per the plan's own instruction). Start-of-session falsification grep (`appcheck`/`app-check`/`app check`/`play.integrity`/`playintegrity` across `.gradle`/`.java`/`.kt`/`.rules`/`.xml`) confirmed the `Open` disposition was accurate — nothing existed. Fix, purely additive, no guard removed: `app/build.gradle` adds `firebase-appcheck-playintegrity` (all build types) + `firebase-appcheck-debug` (`debugImplementation` only); `DuoShieldApp.onCreate()` installs `PlayIntegrityAppCheckProviderFactory` (+ `DebugAppCheckProviderFactory` under `BuildConfig.DEBUG`) before any other Firebase SDK call, wrapped in a fail-open try/catch; `firestore.rules` adds an unreferenced `appCheckVerified()` helper (`request.app != null`), deliberately not wired into any `allow` clause yet (monitoring-first rollout; existing installs need the new APK first; the emulator harness doesn't mint App Check tokens). Enforcement enable (Firebase console) and wiring the helper into real rules are tracked next steps, not done here; sideloaded/rooted-device gap accepted per README's threat model. Verification: `which java firebase gradle` all empty (confirms AND+RULES lanes genuinely unavailable); grep confirms the new import/usage sites; `firestore.rules` brace balance 79/79; `cd server && npm test` 229/230 pass (1 pre-existing unrelated failure, server code untouched — confirms no collateral breakage). No new automated test written — nothing behavioral exists yet for either suite to exercise; the real regression test belongs to whichever future session wires the helper into an `allow` clause. Commit `9b58b3a`. See `sessions/SESSION-S3-15.md`.  **Also done: S3-17** (Android backup & group crypto — `S07-H2`, `S07-H3`, `S07-M2`, `S07-M3`, `S07-L1`, `S07-L4`, `S10-N2`, lane AND (`S07-H3` additionally RULES) — **all 7 land Partial**, verify BLOCKED). Implementation had already landed on this branch (commits `46627a2`/`443820e`/`ad703c9`) before this documentation pass; every change was independently re-verified against current source rather than trusted from the commit messages (source beats tracker). `S07-H2`: `BackupCryptoHelper.computeHmac`/`verifyHmac` (HKDF-derived key with a distinct `info` string from the AES-GCM key, constant-time compare) replaces the unkeyed-SHA-256-checksum oracle; wired into all 4 `BackupManager` write call sites plus the restore path (prefers `verifyHmac`, falls back to legacy `verifyChecksum` only for pre-fix docs with no `hmac` field). `S07-M3`: `BackupCryptoHelper.buildAad(docId, conversationId, compressed)` binds those Firestore metadata fields into the GCM tag via new AAD-aware `encryptCompressed`/`decryptCompressed` overloads, same call sites, same commit. `S07-H3`: `GroupCipherHelper.buildAad(groupId, senderUid, messageId)` binds group ciphertext to its context; `GroupChatActivity`'s send path builds/passes the AAD and tags the doc `aadV1: true`, the receive path reconstructs the identical AAD only when `aadV1` is present so legacy messages (no bound context) are unaffected — crypto-layer fix only, the paired Firestore-rules half of this finding is untouched and routes to **S3-15b**. `S07-M2`: `DuoShieldSignalStore` trust storage re-anchored on the SHA-256 fingerprint of the identity key (`KEY_TRUST_BY_FP_PREFIX`) instead of the mutable per-address uid key; new `readTrustRecord()` one-time-migrates a legacy uid-keyed record forward on first read, migrating the address's own already-trusted key (verified this does not misattribute trust across uids). `S07-L1`: `fetchGroupKey`'s creator check flipped from `creatorUid != null && !creatorUid.equals(sender)` (failed OPEN on a null/missing `creatorUid`) to `creatorUid == null || !creatorUid.equals(sender)` (fails CLOSED). `S07-L4`: `loadSession` now deletes a corrupt session row and throws a new `SessionDeserializationException` instead of silently returning a fresh empty `SessionRecord` (which let `containsSession()` lie and caused encryption to silently run against an empty ratchet); verified every app-level caller already wraps libsignal calls in `catch (Exception ...)`, so this surfaces as a caught, logged failure, not an uncaught crash. `S10-N2`: every raw-peer-uid `Log.w`/`Log.e` call site in the two touched files now routes through the pre-existing `LogRedact.uid()` helper (already used elsewhere, e.g. `SignalSessionManager`); grep confirmed no raw-uid release-surviving log line remains in the two touched files (a full-repo sweep beyond those files was not performed and is not claimed). New unit tests, plain JVM/`src/test`, no Android/libsignal native dependency: `BackupRoundTripTest.java` (HMAC/AAD round-trip) and `GroupCipherHelperAadTest.java` (AAD round-trip + tamper/mismatch rejection). Verification: `which java/javac/gradle` all empty (same AND-lane blocker as every prior Android session); a corrected char-by-char brace/paren/bracket tokenizer (a first regex-based pass gave a false-positive mismatch on `GroupChatActivity.java` from comment/string-stripping order) confirmed all 7 touched/added files BALANCED. None of the 7 promoted to Fixed — AND verification BLOCKED → **S3-19b** for all seven, `S07-H3`'s RULES half additionally BLOCKED → **S3-15b**. See `sessions/SESSION-S3-17.md`.  **Next: S3-18** (next unstarted session in the plan's numeric sequence) — `S3-13` through `S3-17` are all now closed per the entries above, consistent with `START_HERE.md`'s chain state. `S3-15` and `S3-16` are sibling branches that forked from the same parent commit and were developed in parallel without either seeing the other's work; they were reconciled together in an earlier merge, which is why `S3-15`'s commits (`9b58b3a`, `0c1fdbc`) did not appear on this branch until then. `../BUG_TRACKER.md` holds per-finding truth. | all remaining |

> ### Status correction, 2026-08-11 (FINAL VERIFICATION session, protocol §9)
>
> **The banners at the top of this file and the Round 2/3 rows above were stale.** They claimed
> `S07-C1` was open and exploitable and that the Round 2/3 logs did not exist, while the work had in
> fact been done and recorded in the (now-deleted) `FINDING_INDEX.md`. That was narrative lag, not a
> fourth fabrication — but it is corrected here because lag is indistinguishable from fabrication
> until someone spends the budget to check.
>
> **Correction, 2026-08-11 (same day, later pass):** the "all 116 findings dispositioned, no
> Critical/High unfixed" claim that used to follow this note was itself the *fourth* false-progress
> incident — it counted an intent column as a result. `FINAL_SECURITY_REPORT.md` (which made that
> claim) has been deleted. **Authoritative state now lives in
> [`../BUG_TRACKER.md`](../BUG_TRACKER.md)**, and the real Round 3 status is tracked in
> `ROUND3_REMEDIATION_PLAN.md`. What genuinely holds: `cd server && npm test` → **153 tests / 153
> pass / 0 fail** (Rounds 1–2 only); Round 3 (the bulk of the findings) remains open.
>
> **Not done, and not counted as done:** 8 operator actions — above all **revoking the leaked GCP
> service-account key** — plus `SC-12` branch protection (re-checked this session: still
> `404 Branch not protected`). 3 verification gaps remain: **Android has never been compiled**
> (no JDK/SDK), `firestore-tests/rules.test.js` has **never been executed** (no `firebase` CLI), and
> no runtime/integration testing was performed. **The program is NOT signed off.**
>
> ### Progress update, 2026-08-11 (Round 3 execution — see `START_HERE.md` chain state)
>
> Round 3 implementation has since **begun**: sessions **S3-01 through S3-07 are complete** (Round 03
> row above; `sessions/SESSION-S3-*.md`). `S3-07` was initially only 3 of its 5 findings fixed under
> an explicit user-set session budget (`S04-M1`/`S04-L3` deliberately deferred, not blocked by a
> toolchain), but both deferred findings were fixed in follow-on sub-sessions before this row was
> finalized — see `sessions/SESSION-S3-07.md` for the full three-sub-session record, including a
> correction note where that file itself had briefly lagged behind the actual fix state. The "Round 3
> was never implemented / remains open" statements above are the frozen 2026-08-11 verification-pass
> record and are **superseded for those sessions only** — the other 12-13 sessions and both catch-up
> gates (S3-15b RULES, S3-19b Android) are still open, and `SC-03` is **partial** (Gradle
> dependency-verification scaffold committed; hash population BLOCKED on toolchain), same as
> S3-04/S3-05 (RULES lane, verify BLOCKED — no `java`/`firebase` CLI this session), not fixed. S3-06
> and S3-07 are the first Round-3 sessions whose lane (`SRV`) had no toolchain blocker, so their
> fixed findings are recorded `fixed`, not `partial`. For "what's next," trust `START_HERE.md`'s
> `NEXT SESSION` line (currently **S3-08**) and `../BUG_TRACKER.md` for per-finding truth. The
> program is still **NOT signed off**.

Relationship to audit sessions: the audit's `SESSION-00…10` are **discovery** sessions (frozen,
immutable). These remediation `SESSION-01…03` are **fix** sessions and are the only ones this
program writes to. They are numbered independently and must not be confused with the audit's.

A round's log file (e.g. `SESSION-01.md`) may itself be revised in place across multiple working
sessions as more of that round's clusters get done — it is not one-log-per-one-sitting. Each
revision must follow `SESSION_PROTOCOL.md` §4 (verify from source before writing a disposition).
