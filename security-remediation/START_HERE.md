# START HERE

**You said: "head to this folder, open this md, and start working." This is that md.**
Do exactly what is below, in order. Do not improvise a different plan. Do not declare anything
`fixed` without running the verification named for it.

---

## What this program is

Rounds 1–2 of the security remediation are code-complete and server-test-verified (server suite
**153/153**). **Round 3 — 103 open findings — is not implemented.** Those 103 findings are scheduled
across **20 sessions** in [`ROUND3_REMEDIATION_PLAN.md`](./ROUND3_REMEDIATION_PLAN.md). One session =
**fix + verify + document** for one scheduled batch. `FINAL_SIGNOFF.md` is **PENDING** and stays that
way until every session and both catch-up gates (S3-15b, S3-19b) have actually run.

---

## Chain state — the ONE line that says what to do next

<!-- Update ONLY this block at the end of each session. It is the single source of truth for "next". -->

```
NEXT SESSION: S3-19 (IN PROGRESS — 2 of 7 plan-scoped findings done: S08-M1, S08-M2. Remaining 5: S08-L1 (validate deep-link Account ID), S08-L2 (EXTRA_IS_SENSITIVE on clipboard), S08-L3 (don't store PIN length beside hash), S08-I1 (stop R8 keeping crypto.**/security.** names), S06-L4 (AccountLockWorker report real failure + cap 5xx retries) — the S3-19 scope in ROUND3_REMEDIATION_PLAN.md is S08-M1/S08-M2/S08-L1/S08-L2/S08-L3/S08-I1/S06-L4; S08-M3 is a separate Accepted disposition and is NOT S3-19 scope). S08-M1 (commit 51f5573): AndroidManifest.xml's <application> tag no longer sets android:allowNativeHeapPointerTagging="false" — the attribute is deleted outright, restoring the platform default (true), re-enabling a memory-safety mitigation for native code (libsignal/WebRTC/SQLCipher) that parses attacker-controlled bytes. S08-M2 (commit 51f5573): file_paths.xml's four whole-directory-scoped FileProvider roots (two external roots the app never writes to, an unscoped files-path root never passed to FileProvider, and a whole-cache-root cache-path that scoped every grant to the plaintext Glide disk cache too) collapsed to one narrowly-scoped root, getCacheDir()/shared/ (new util/SharedCacheDir.java, split into camera/media/export subdirectories); the 4 call sites that create FileProvider-shared files (ChatMediaActivity, GroupChatActivity, SecureShareHelper, ChatExportHelper) now write into it instead of the cache root; TempFileCleaner's sweep was extended (sweepDir() extracted, called a second time over the new shared/ subdirectories) so relocating those files didn't silently stop them from being cleaned up (would have reopened S08-H3). Both land Partial: source-reviewed, xmllint-validated, Java balance-checked, not compiled — S3-19b. New regression coverage: S319ManifestTest.java (plain JVM XML-parse test) for both findings. Neither catch-up gate has run: S08-H2/S08-H3/S08-L4 (from S3-18) plus this session's S08-M1/S08-M2 all have AND-lane compile/test verification BLOCKED (no JDK/Gradle/Android SDK) and routed to S3-19b.
PRIOR NEXT (superseded by the line above): S3-19 — S3-18 is now scope-complete. All three plan-scoped findings (S08-H2, S08-H3, S08-L4) are source-fixed/source-reviewed and land Partial; the S08-H3 "sweep 4 temp prefixes" sub-item (commit 5171495) and S08-L4's recents-exclusion fix (commit e284fb5) — both carried forward from the prior S3-18 pass below — are now landed. Neither catch-up gate has run: all of S08-H2/S08-H3/S08-L4's AND-lane compile/test verification is BLOCKED (no JDK/Gradle/Android SDK) and routed to S3-19b — S3-19 may proceed on its own scope while that stays outstanding.
LAST DONE:    S3-18  continuation — closed the remaining S3-18 scope. (1) S08-H3 "sweep 4 temp prefixes" sub-item (commit 5171495, landed before this pass, re-verified this pass): TempFileCleaner.isTempMediaFile() extended to cam_*.jpg / grp_cam_*.jpg (the genuine leak — plaintext camera captures handed to the camera app via FileProvider, never deleted by the app itself) and thumb_*.mp4 / enc_*.tmp (crash-orphan defense for scratch normally cleaned in a finally block); vid_view_*.mp4 needed no new rule (already matches the vid_ prefix); b2dl_*.enc deliberately not added (lives inside the chat_export_* working dir, already covered by that directory's recursive sweep). (2) S08-L4 (commit e284fb5, this pass): AndroidManifest.xml's .LockScreenActivity entry gained android:excludeFromRecents="true" and android:launchMode="singleTask" — the literal "neither is excluded from recents" gap. The finding's other requested change ("set FLAG_SECURE on it rather than clearing it") was already closed as a side effect of S08-H2's earlier fix in this same S3-18 session. NOT attempted: reordering the lock decision from onStart into onCreate (or an immediate opaque overlay) — the wider blast radius across every BaseActivity subclass cannot be regression-tested without a compiler here; the residual gap this leaves is narrow (a brief pre-cover moment, only reachable via a recents capture, only when the user has explicitly opted out of the secure-by-default screenshot preference). New regression coverage: LockScreenManifestTest.java (plain JVM XML-parse test, no Android SDK needed) asserts both manifest attributes; hand-verified against the live manifest with `xmllint --xpath` (exact match) but not compiled (no JDK). BUG_TRACKER.md S08-L4 row: Open → Partial (S3-18). S08-H3 row already carried the temp-sweep detail from the prior pass. Toolchain: no JDK/Gradle/Android SDK in this environment — source-reviewed and structurally checked, not compiled; AND-lane verification for all of S08-H2/S08-H3/S08-L4 remains routed to S3-19b. See SESSION-S3-18.md's "Continuation" section.
LAST DONE:    S3-18  Android platform privacy (S08-H2, S08-H3, S08-L4), lane AND (verify BLOCKED → S3-19b) — PARTIAL. Implementation (bdcad65) and the S08-H2/S08-H3 tracker rows (4a80703) had already landed on this branch before this documentation/verification pass; this session independently re-verified both findings against current source (source beats tracker) rather than trusting the commit messages, ran the available structural checks, verified the AndroidManifest backup config, and wrote the session/index/chain-state documentation the interrupted run had not. S08-H2 (FLAG_SECURE cleared app-wide) → Partial: new BaseActivity.applyScreenshotSecurity(Activity) reads app_screenshot_enabled (default false = secure) and applies/lifts FLAG_SECURE; MainActivity + LockScreenActivity (both extend AppCompatActivity directly) route their onCreate() through it; SecurityPrivacySettingsActivity.applyScreenshotFlag(allow) now honours its allow parameter instead of unconditionally clearing; no unconditional FLAG_SECURE clear remains anywhere (grep-confirmed). S08-H3 (plaintext media in the 150 MB Glide disk cache) → Partial: MessageDao.deleteExpired() now returns int (Room affected-row count), SelfDestructWorker propagates the Room+Firestore delete counts and calls Glide.clearDiskCache() — off the main thread, wrapped non-fatal — ONLY when a pass actually deleted ≥1 message (zero-expired poll evicts nothing; no legitimate Glide caching path disabled). Both land Partial (not Fixed): AND-lane compile/instrumentation BLOCKED here (which java/javac/gradle all empty) → S3-19b; brace/paren/bracket balance PASS on all 6 touched files. Backup check: AndroidManifest allowBackup="false" + fullBackupContent="false" confirmed — no backup-exposure claim made. NOT done, carried forward as remaining S3-18: S08-L4 (recents exclusion) and the S08-H3 temp-prefix sweep (TempFileCleaner coverage gap on cam_/enc_/vid_view_/thumb_/b2dl_). Documentation commit separate from implementation. See SESSION-S3-18.md.
LAST DONE:    S3-17  Android backup & group crypto cluster (S07-H2, S07-H3, S07-M2, S07-M3, S07-L1, S07-L4, S10-N2), lane AND (S07-H3 also RULES) — all 7 Partial, verify BLOCKED. Implementation had already landed on this branch before this documentation pass started (commits 46627a2/443820e/ad703c9); this session independently re-verified every change against current source (source beats tracker) rather than trusting the prior commit messages, then closed out the tracker/session documentation. S07-H2: BackupCryptoHelper.computeHmac/verifyHmac (HKDF-derived key, distinct info string from the AES-GCM key, constant-time compare) replaces the unkeyed SHA-256 checksum oracle; wired into all 4 BackupManager write call sites + the restore path (verifyHmac preferred, legacy verifyChecksum only for pre-fix docs with no hmac field). S07-M3: BackupCryptoHelper.buildAad(docId, conversationId, compressed) binds those Firestore metadata fields into the GCM tag via encryptCompressed/decryptCompressed AAD overloads, same call sites, same commit. S07-H3: GroupCipherHelper.buildAad(groupId, senderUid, messageId) binds group ciphertext to its context; GroupChatActivity's send path builds/passes the AAD and tags the doc aadV1:true, the receive path reconstructs the identical AAD only when aadV1 is present (legacy messages unaffected); crypto-layer fix only — the paired Firestore-rules half of this finding is untouched, routed to S3-15b. S07-M2: DuoShieldSignalStore trust storage re-anchored on SHA-256 fingerprint of the identity key (KEY_TRUST_BY_FP_PREFIX) instead of the mutable per-address uid key; readTrustRecord() one-time-migrates a legacy uid-keyed record forward to fingerprint-keyed storage on first read, migrating the address's own already-trusted key (verified this does not misattribute trust across uids). S07-L1: fetchGroupKey's creatorUid check flipped from creatorUid != null && !creatorUid.equals(sender) (fails OPEN on null creatorUid) to creatorUid == null || !creatorUid.equals(sender) (fails CLOSED). S07-L4: loadSession now deletes a corrupt session row and throws a new SessionDeserializationException instead of silently returning a fresh empty SessionRecord (which let containsSession() lie and caused encryption to silently run against an empty ratchet); verified every app-level caller already wraps libsignal calls in catch(Exception), so this surfaces as a caught, logged failure, not an uncaught crash. S10-N2: every raw-peer-uid Log.w/Log.e call site in the two touched files now routes through the pre-existing LogRedact.uid() helper (not new — already used elsewhere, e.g. SignalSessionManager); repo-wide grep confirmed no raw-uid release-surviving log line remains outside a case-by-case review of the two touched files (full-repo sweep beyond those files not claimed). New unit tests (plain JVM, no Android/libsignal native dependency): BackupRoundTripTest.java (HMAC/AAD round-trip) and GroupCipherHelperAadTest.java (AAD round-trip + tamper/mismatch rejection). Verification this session: which java/javac/gradle all empty (confirms AND-lane blocker, same as every prior AND-lane session); a proper char-by-char brace/paren/bracket scanner (not a naive regex — a first regex-based pass gave a false-positive MISMATCH on GroupChatActivity.java caused by comment/string-stripping order, corrected with a real tokenizer) confirmed all 7 touched/added files BALANCED. None of the 7 findings promoted to Fixed — AND verification BLOCKED -> S3-19b for all seven, and S07-H3's RULES half additionally BLOCKED -> S3-15b. See SESSION-S3-17.md.
LAST DONE:    S3-16  OUT-OF-ORDER, lane AND (verify BLOCKED → S3-19b) — Android crypto storage cluster (S08-H5, S07-M1, S07-L2, S07-L3). Ran ahead of the plan's numeric sequence at explicit user direction; S3-15 (App Check + client provider wiring, S10-N1) is STILL the plan's actual next unstarted scheduled session and was NOT touched by this run — same out-of-order precedent as S3-13's earlier pickup (see below). Source-verified all 4 findings against current code before changing anything (source beats tracker). S08-H5/S07-M1 (SecurePrefs plaintext fallback holding the identity key, backup key, and SQLCipher passphrase) re-confirmed genuinely OPEN, not code-changed: the plan's literal sub-claim ("isInitialized() must not ignore fallback") is confirmed already correct and deliberate (SignalKeyManager.isInitialized() reads via SecurePrefs.get(ctx) regardless of tier, explicitly not gated on isAvailable() — an in-code comment cites BUG-S10, a prior permanent sign-in-loop regression on fallback devices) — not a regression to fix. The root defect remains open: BackupCryptoHelper.storeKey() still writes the backup key into the plaintext fallback unconditionally while getStoredKey() refuses to read it back once !isAvailable(), leaving an unused plaintext key copy on disk either way; closing this needs a product-level UX/consent decision (warn-before-fallback, or refuse to persist the backup key at all and silently break backup on affected devices), out of scope for an AND-lane source-only session. S07-L2 (static derivationCache in SeedPhraseHelper surviving every wipe path) and S07-L3 (mnemonicToSeed not self-canonicalising case/whitespace, plus two locale-sensitive .toLowerCase() calls) were both source-fixed: new SeedPhraseHelper.clearDerivationCache() wired into WipeHelper.eraseLocalData() Step 4 for all WipeMode values; new SeedPhraseHelper.canonicalizeMnemonic() (trim + Locale.ROOT lower-case + whitespace collapse) called from inside mnemonicToSeed(), plus validateMnemonic()/RestoreFromSeedActivity's existing .toLowerCase() calls switched to Locale.ROOT explicitly; 4 new regression tests added to SeedPhraseHelperTest.java (plain JUnit, no libsignal native dependency) — 3 canonicalisation tests against a known BIP39 vector (case-insensitivity, whitespace-collapse, whitespace-trim, all asserting byte-identical seeds) and 1 reflection-based test proving clearDerivationCache() actually nulls the backing field. **Status: Partial, not Fixed** — no JDK/Gradle/Android SDK exists in this environment (which java/javac/gradle all empty on PATH), so the actual compile and unit/instrumented test execution has never run; a brace/paren/bracket-balance check (python3) on all 4 touched files was run as a structural sanity check only, not a substitute for a compile. Per the Round-3 plan's own exit criterion for this session ("source-reviewed; lands partial — AND verification BLOCKED pending S3-19b"), both fixes are recorded Partial and promoted to Fixed only after S3-19b actually compiles and runs them. Commit 3ef358e (implementation: SeedPhraseHelper.java, WipeHelper.java, RestoreFromSeedActivity.java, SeedPhraseHelperTest.java) plus a separate documentation-only commit for BUG_TRACKER.md/START_HERE.md/SESSION_INDEX.md/SESSION-S3-16.md. See SESSION-S3-16.md for full detail.
LAST DONE:    S3-15  PARTIAL — S10-N1 (Firebase App Check absent) client provider wiring + rules scaffold, lane AND/RULES verify BLOCKED → S3-15b/S3-19b, NOT promoted to fixed here per the plan's own instruction. Start-of-session falsification check (grep -rni "appcheck|app-check|app check|play.integrity|playintegrity" across .gradle/.java/.kt/.rules/.xml) confirmed the tracker's `Open` disposition was accurate — zero prior wiring existed anywhere. Fix: app/build.gradle adds firebase-appcheck-playintegrity (all build types) + firebase-appcheck-debug (debugImplementation only, never ships in release); DuoShieldApp.onCreate() installs PlayIntegrityAppCheckProviderFactory (+ DebugAppCheckProviderFactory under BuildConfig.DEBUG) before any other Firebase SDK call in the method, wrapped in a fail-open try/catch (App Check is cost-raising attestation, not the app's only control — a provider-install failure on an unsupported device must not crash startup); firestore.rules gets a new appCheckVerified() helper (request.app != null), deliberately NOT wired into any allow clause yet — per the audit's own monitoring-first recommendation, because every existing Android install needs the App-Check-aware APK before any rule could require request.app without locking out real traffic, and firestore-tests/rules.test.js's emulator harness does not currently mint App Check tokens (would fail every existing case if wired in now). Enforcement enable (Firebase console monitoring→Enforce) and wiring appCheckVerified() into real allow clauses are tracked as the next steps, not done here; the sideloaded/rooted-device residual gap is accepted per README.md's own threat model (a compromised client bypasses attestation by definition). No existing guard removed or weakened — purely additive. Verification this session: which java/firebase/gradle all empty (confirms AND+RULES lanes genuinely unavailable here, not just assumed); grep confirms the 3 new import/usage sites in DuoShieldApp.java + 2 dependency lines in build.gradle; firestore.rules brace balance 79/79 (Python count); cd server && npm test 229/230 pass (1 pre-existing identityVerify.test.js native-module failure, not a regression — server code untouched this session, confirms no collateral breakage). No new automated test was written: the change has nothing for the server suite to exercise (no server code touched) and nothing behavioral for the rules emulator to assert yet (the helper is unreferenced by any allow clause, and the emulator doesn't exist in this environment regardless) — the meaningful regression test ("enforcement actually blocks a token-less request") belongs to whichever future session wires appCheckVerified() into an allow clause, not this scaffold session. Commit 9b58b3a (implementation + BUG_TRACKER.md + SESSION-S3-15.md). See sessions/SESSION-S3-15.md for full detail.
LAST DONE:    S3-14  COMPLETE — all 6 plan-scoped findings (S05-L1, S05-L2, S05-L3, S05-L4, S05-I2, S05-I3) Fixed/Verified, lane SRV. Resumed an interrupted run: S05-L1/L2/L3/L4 were already implemented + tracker-recorded + tested (8 structural tests in server/lib/s3_14_admin_remediation.test.js + pure.test.js halves) from PR #82 — re-verified from current source this session (validAdminUid(uid) on all 4 uid-taking admin routes incl. account/lookup:4449; requireAdminAuthThenBody auth-before-collectBody on all 6 POST routes + server.headersTimeout=15s/requestTimeout=30s; Cache-Control:no-store + Vary:Cookie for /admin* in setBaselineSecurityHeaders; approve/unfreeze wrapped in db.runTransaction with unfreeze re-checking locked===true inside the txn; GET /admin/api/locked + /duress/enrolled both .limit(500)) and NOT re-touched. S05-I2 + S05-I3 were implemented in source but still recorded Open/Carried and the S05-I3 Sec-Fetch-Site regression test had not been added — that was the exact stop point. This session: (1) added the finishing regression tests to s3_14_admin_remediation.test.js — "requireAdminAuth rejects Sec-Fetch-Site: cross-site with a 403 before any auth work" (asserts exact "cross-site" match, 403, auditAdminEvent("admin_api_blocked_cross_site"), AND that the reject precedes safeTokenEqual so a forged cross-site request never reaches the credential path) and a SameSite=Lax comment-accuracy test; (2) caught + corrected a THIRD stale admin comment the earlier S05-I2 pass missed — requireAdminAuth's CSRF note claimed SameSite=Strict "already blocks the cross-site case," but the cookie is issued SameSite=Lax (Strict is unusable: some Android in-app webviews drop it on the post-login top-level navigation); the comment now accurately says Lax (which still withholds the cookie on cross-site sub-requests, the mutating-call case), keeping the S05-I3 defense-in-depth rationale honest. No logic change, comment-only edit to index.js. S05-I3's cookie-Secure trust boundary (x-forwarded-proto honored only when TRUSTED_PROXY_HOPS>0, plus a FORCE_SECURE_COOKIES override) and safeTokenEqual length-oracle removal were verified already-correct from source and not re-touched. Lane SRV ran: node --check index.js/pure.js/s3_14 test clean; node --test s3_14_admin_remediation.test.js 11/11; npm test 229/230 pass (baseline was 227/228 — the +2 are this session's new tests; the 1 failure is the same pre-existing unrelated identityVerify.test.js missing-native-module issue documented since S3-05, not a regression). Commit 3ef65c2 (implementation + tests); documentation commit recorded in SESSION_INDEX.md. See SESSION-S3-14.md for full detail.
LAST DONE:    S3-13  S05-M3 FIXED — admin session absolute lifetime + IP/UA binding + bulk revoke; authenticated refresh. **Correction to the prior "Partial" disposition:** ADMIN_SESSION_TTL_MS (30 min) was never an absolute ceiling — it was purely a sliding idle timeout, refreshed even by the unauthenticated GET /admin render check, so an idling-open tab never actually expired. Fix: new server/lib/adminSessionStore.js splits idle (ADMIN_SESSION_IDLE_TTL_MS, 30 min, refreshed on real auth calls) from a fixed absolute ceiling (ADMIN_SESSION_ABSOLUTE_TTL_MS, 8h, set once at creation) that refresh can never push past (Math.min(now + idleTtlMs, absoluteExpiresAt)); sessions are now bound to ip: ipTag(getClientIp(req)) (the same pseudonymised tag S05-M1 writes to the audit log) and userAgent at creation, and validate() rejects a context mismatch as ip_mismatch/ua_mismatch without deleting the session (so a legitimate IP/carrier change doesn't force a re-login, while a replay from elsewhere is still blocked); GET /admin now calls hasValidAdminSession(req, { refresh: false }) so page loads alone can no longer extend a session; new POST /admin/api/sessions/revoke-all (admin-gated, audits admin_sessions_revoked_all, clears the caller's own cookie too) plus a "Sign out everywhere" admin-panel button; logout now calls adminSessionStore.revoke(sessionId). Evidence: server/lib/adminSessionStore.test.js (15 new tests: normal validity, refresh-before-expiry, refresh-capped-at-absolute-ceiling via the store's _get() accessor, IP/UA-mismatch handling non-destructive, revoked-session rejection, revokeAll() bulk invalidation incl. empty-store case, legitimate multi-refresh continuity); adminAuditWiring.test.js (+4 source-wiring checks + admin_sessions_revoked_all added to the required-audit-actions list). node --check index.js clean; npm test 216/217 pass (198 baseline + 19 new; 1 failure is the same pre-existing unrelated identityVerify.test.js native-module issue, not a regression). See SESSION-S3-13.md's "S05-M3" section for the full session-lifecycle trace done before editing, and the residual limitations (in-memory store, absolute TTL not yet env-configurable, no auto-revoke-on-mismatch) documented there.
LAST DONE:    S3-13  E2E-check (no code changes) — re-traced S05-H2's deny path from the admin-panel "Deny" button down to the Firestore write and back, confirming every hop is real (not a mock/stub/no-op): denyBtn.onclick -> deny() -> api() (sends x-admin-token + same-origin session cookie) -> POST /admin/api/waitlist/deny -> requireAdminAuth() (the same gate every other /admin/api/* route uses, with IP lockout + admin_api_unauthorized audit on failure) -> ref.update({ status: "denied", deniedAt: ... }) on the real waitlist/{requestId} doc -> auditAdminEvent("waitlist_denied", ...) (pseudonymised IP, reqTag(requestId), durable adminAuditLog write, client-read/write denied by firestore.rules) -> GET /waitlistStatus echoes "denied" verbatim -> /mintToken's status !== "approved" check already rejects it, no denied-specific carve-out needed. git status was clean at start and stayed clean (no code changes); npm test re-run 197/198 pass, same pre-existing unrelated identityVerify.test.js native-module failure. No new regression test added — the existing adminAuditWiring.test.js S05-H2 structural test already covers the properties that matter and no gap was found. S05-H2 stays Partial (unchanged disposition; this was a depth-check, not new scope). See SESSION-S3-13.md's "Continuation session — E2E-check" section for full detail.
LAST DONE:    S3-13  continuation — finished S05-H2 (Partial: new POST /admin/api/waitlist/deny mirrors /approve's shape, sets a new status: "denied", audited via auditAdminEvent("waitlist_denied"); admin panel gained a Deny button. Not Fixed: the finding's full design-inversion fix, invite expiry, revoke-an-approval, and pagination are still open — this closes only the literal "no deny path" gap S3-13's exit criteria names) and S05-M1 (Fixed, correcting a false "Partial" disposition inherited from the prior out-of-order S3-13 session: source-checking before touching anything found FOUR direct db.collection("adminAuditLog").add(...) call sites — waitlist_approved, account_unfrozen, duress_enrolled, duress_revoked — that bypassed the auditAdminEvent() sink entirely and wrote the RAW adminIp, contradicting the prior claim that "7 call sites" funneled through it; duress_enrolled/duress_revoked also still wrote the raw uid into that direct write despite their console.log already being pseudonymised in S3-09. All four now route through auditAdminEvent(action, req, extra) — no signature change needed, it already accepted extra fields — closing the raw-IP gap for all four and, for the two duress actions, switching to uid: uidTag(uid) to close the finding's own "most sensitive" callout; account_unfrozen deliberately keeps its raw uid per a pre-existing documented tradeoff, untouched. New reqTag() helper (same pepper-HMAC scheme as ipTag/uidTag) closes the last literal item, the two requestId=${requestId} console.log lines). Lane SRV ran: node --check index.js clean; npm test 197/198 pass (1 pre-existing identityVerify.test.js native-module failure, not a regression); new tests in adminAuditWiring.test.js (direct-write-count regression, duress-uid-pseudonymisation regression, requestId-clear-text regression, S05-H2 deny-path structural wiring). S05-M3 deliberately left untouched — needs a new admin-sessions list/store shape this session didn't have budget to design and test carefully; deferred whole, not partially started. See SESSION-S3-13.md's "Continuation session" section for full detail.
LAST DONE:    S3-12  FIXED (continuation 2, closing) — all 6 of the original 6 bundled findings are now closed from source; S3-12 as a whole is done for what this environment can verify. This session finished the 2 findings left open by the two prior S3-12 sessions: S03-L3/S04-I2 (dead B2 presign surface) and S10-N3 (cold-tier migration race). Discovered on state-recovery that an earlier, undocumented commit (e366730) had already done most of the work — removed b2PresignUrl/b2PresignUrlForUid from server/index.js, removed buildB2PresignUrl/b2HmacKey + their exports from server/lib/pure.js, removed the dead b2PresignedPut/b2PresignedGet/b2Delete AUTH_RATE_LIMITS entries, and added the S10-N3 race guard + its 2 regression tests to worker/src/index.js (worker suite 27/27 -> 29/29) — but left it undocumented and left orphaned test/doc references behind. This session: (1) repo-wide grep confirmed b2PresignUrl/b2PresignUrlForUid/buildB2PresignUrl/b2HmacKey have zero live references left (only removal-comments and unrelated historical docs); (2) deleted 8 now-orphaned tests in server/lib/pure.test.js that called the removed buildB2PresignUrl/b2HmacKey exports (would throw, not just fail, if run); (3) removed the stale B2_KEY_ID/B2_APPLICATION_KEY/B2_BUCKET/B2_REGION doc row from server/README.md — server/index.js no longer reads any of the four; (4) fixed a stray indentation artifact in server/index.js left over from e366730 (cosmetic, no logic change); (5) re-read the S10-N3 race guard end-to-end and confirmed it's correctly scoped (only fires when the post-PUT R2 re-HEAD comes back null, i.e. a concurrent client DELETE won the race) and doesn't touch the two other unrelated race guards in the same function — no code change made to it. S03-L3/S04-I2 now Fixed (operator action still outstanding: revoke the leaked Backblaze B2 key at Backblaze — cannot be done from source). S10-N3 stays Partial, not Fixed: code/tests are verified (worker suite 29/29, including the 2 migration-race tests), but production runtime verification against a live Cloudflare Worker + R2 + B2 deployment remains BLOCKED in this environment (no wrangler/live account access). Lane WORKER ran: node --test src/index.test.js 29/29 pass (via an isolated /tmp node_modules install + symlink, since a direct npm install inside worker/ hit the same package-firewall block noted in the first S3-12 pass; symlink removed after the run, confirmed by git status --porcelain showing no test-artifact changes). Lane SRV ran: node --test lib/pure.test.js 60/60 pass; node --check on server/index.js, server/lib/pure.js, worker/src/index.js all clean. Commit (implementation, message beginning "S3-12: finish S03-L3/S04-I2") — see git log for the exact hash; documentation committed separately per SESSION_PROTOCOL. See SESSION-S3-12.md's "Continuation 2" section for full detail.
LAST DONE:    S3-12  PARTIAL (continuation) ��� picked up 3 of the 5 findings left open by the prior S3-12 partial session (which fixed S03-M2 only): S03-M1, S03-M3, S03-L2. Plus a separate, previously-flagged latent bug: the S03-H3 follow-up (same-holder overwrite quota double-counting). S03-M1 (attacker Content-Type, no nosniff/Content-Disposition) FIXED: PUT/GET on both tiers now derive the served Content-Type purely from the already-allow-listed key extension (new contentTypeForKey()) instead of reading/replaying the client's Content-Type header at all; both GET paths gained X-Content-Type-Options: nosniff and Content-Disposition: attachment. S03-M3 (10-min unrevocable bearer tokens, unlimited reuse) PARTIAL — wire format grows a jti segment (v1.<op>.<expiresAt>.<uidTag>.<jti>.<sig>, 6 parts; old 5-part shape now rejected as malformed) minted by server/index.js's signMediaToken() and verified by the Worker; delete tokens (the one verb with no undo) are now tracked single-use in RATE_KV and 403 on replay — read/write remain reuse-permitted (deliberate: normal client behavior) and the 10-min TTL itself was left unchanged (deliberate: matches realistic client timing, judged lower-value than closing the delete-replay gap). S03-L2 (unguarded decodeURIComponent) FIXED: wrapped in try/catch, returns the standard 400 shape instead of an uncaught URIError. S03-H3 follow-up (same-holder overwrite quota double-counting) FIXED: pre- and post-upload quota checks and the final credit now compute a delta (existing object's HEAD size subtracted from the new size) instead of crediting the full new size on top of the old every time; documented (not fixed) residual limitation: still non-atomic across concurrent overwrites of the same key, same as every other KV-counter in this file (see S03-I2), and a rejected overwrite cannot roll back the destructive put() it already performed (no R2 object versioning configured). Lane WORKER ran: node --check src/index.js AND server/index.js both clean; cd worker && npm test (node --test) 27/27 pass (17 pre-existing + 10 new: 2 for S03-M1, 3 for S03-M3, 2 for S03-L2, 3 for the S03-H3 follow-up). Commits 7e6b95c (worker implementation, auto-committed), ecd5199 (server companion: signMediaToken jti). See SESSION-S3-12.md for full detail, including the environment note on a fresh-VM npm-install blocker (wrangler's devDependency tree fetch failed through a package-firewall proxy; --omit=dev installed the one runtime dependency, aws4fetch, cleanly — not a code defect).
              S3-12  PARTIAL (first pass) — user explicitly scoped this session to "one more bug," picked S03-M2 out of S3-12's 6-finding bundle (S03-M1/M2/M3/L2/L3+I2/S10-N3 offered as options). S03-M2 (tokens scope-bound, not uploader-bound) FIXED: verifyMediaToken() proved chat/group membership only, never object ownership, so any participant of a conversation could mint a genuine, correctly-signed write/delete token for a key ANOTHER participant uploaded and silently overwrite or delete their media. Fix in worker/src/index.js — PUT now head()s the existing key before writing and 403s if customMetadata.uploader differs from the caller's token holder (first write to a brand-new key is unaffected, nothing to protect yet); DELETE (R2 tier) applies the identical check by reusing the head() already done for S03-H3's quota accounting; DELETE (B2/cold tier) adds a new HEAD-before-DELETE step reading x-amz-meta-uploader, 403ing on mismatch, and falling through unchanged on 404 or on a legacy object with no uploader tag at all (left deletable rather than permanently stuck); the scheduled() R2->B2 migration cron now carries customMetadata.uploader across as x-amz-meta-uploader so the B2-side check has something to read for objects migrated after this fix. GET (read) is deliberately left scope-bound only — every participant should be able to read every message's media. Lane WORKER ran: node --check src/index.js clean; cd worker && npm test (node --test) 17/17 pass (9 pre-existing from S3-11 + 8 new: non-uploader denied on PUT-overwrite/DELETE on both tiers, genuine uploader still succeeds on both tiers, first-write-to-new-key unrestricted, untagged-legacy-B2-object still deletable). Commit 9b53f24 (implementation). The same-holder-overwrite double-counting gap flagged here was fixed in the continuation session above.
LAST DONE:    S3-11  FIXED — all 5 plan-scoped findings (S03-H2, S03-H3, S03-I2, S03-L4, S08-I3) verified Fixed from current source. S03-H2/S03-I2 (per-user, not per-token, rate bucket / accounting) were already fixed by a prior corrected session — re-confirmed from source (credentialBucketKey()/checkPerUserRateLimit(), index.js:288-316: per-minute bucket keyed by a SHA-256 hash of the caller's Authorization header, with stale-minute pruning), not re-touched. S03-H3 (per-user R2 storage quota), S03-L4 (CORS on every rejection path), and S08-I3 (drop ACAO: * while allowing Authorization) were implemented by a prior in-flight (uncommitted-at-handoff) session and completed/verified this session: per-user byte counters gated by configurable MAX_USER_BYTES, uploader holder ID stored as R2 metadata so DELETE/migration credit the right account back, every response path (success + 400/401/403/404/413/429/502/507) flows through one respond() closure carrying a non-wildcard, allow-listed cors value. This session's own contribution: (1) actually ran the tests — found and fixed 3 environment-only blockers (aws4fetch installed as an empty dir, Node's crypto.subtle lacking the Workers-runtime-only timingSafeEqual extension, getB2Client() needing non-empty dummy B2 credentials to construct) — none are implementation defects; (2) found and closed a genuine S03-H3 gap during the mandated quota review: the pre-upload check only bounds the client-declared Content-Length, so a missing/understated header (e.g. a streamed body) could store an oversized object while under-crediting the quota — added a post-upload re-check against the true R2 head() size (mirroring the existing file-size guard's delete-then-507 shape) before any quota credit, with a new regression test proving the bypass is closed. Lane WORKER ran: node --check src/index.js clean; cd worker && npm test (node --test) 9/9 pass (8 pre-existing + 1 new). Commit b6faf6a (implementation). See SESSION-S3-11.md for full detail, including the full concurrency/accounting review checklist.
LAST DONE:    S3-10  FIXED — all 3 plan-scoped findings (S05-M2, S06-M1, S06-L3) verified Fixed from current source; none needed new production code. S05-M2/S06-M1 (duressEligibility enforced nowhere but a cached client bool) were already remediated by pre-existing commit adaa218: server/index.js's /requestLockNonce gates on duressEligibility/{uid}.eligible before issuing a nonce (404, not 403, for enumeration resistance), and firestore.rules' accountLock create rule requires the same predicate — but neither had a regression test. S06-L3 (_duressNonces rules-test coverage) was already remediated by pre-existing commit 1a0e15b: firestore-tests/rules.test.js carries 8 cases for _duressNonces and 6 for waitlist, exceeding the finding's 4-case ask. This session's only new artifact: server/lib/duressEligibilityWiring.test.js (5 cases, structural source-level test guarding the S06-M1/S05-M2 gate), falsified by temporarily reverting the firestore.rules clause and confirming the test fails, then restoring the file byte-for-byte before committing. Lane SRV ran: npm test 200/201 pass (server suite); the 1 failure is the same pre-existing identityVerify.test.js missing-native-module issue documented since S3-05 through S3-13, not a regression. RULES lane emulator execution remains BLOCKED (no firebase/java in this environment) — promotion of the *execution* result routes through S3-15b, same as this tracker's other rules rows. Commit e113d21 (implementation + tracker). See SESSION-S3-10.md for full detail.
              S3-13  OUT-OF-ORDER PARTIAL — ran ahead of the plan's numeric sequence (before S3-10/S3-11/S3-12) because the prior session (S3-09) had already partially advanced S05-M1 as a side effect and left it explicitly flagged as carried-forward scope. Picked up exactly the one remaining well-scoped item: auditAdminEvent() (the single Firestore-audit sink all 7 admin-audit call sites funnel through) now writes ipTag(getClientIp(req)) instead of the raw operator IP into the permanent, no-TTL adminAuditLog collection, closing S05-M1's most-cited concern. A Firestore TTL/retention policy was deliberately rejected as the fix, per the finding's own tension note with S05-H3 (durable-but-pseudonymised, not durable-but-deleted). S05-M1 stays Partial, not Fixed — the requestId=${requestId} clear-text lines are still open. S05-H2 (waitlist deny/expire/revoke) and S05-M3 (admin session bulk-revoke + IP/UA binding) were read only far enough to confirm they were out of this one-bug scope; both remain as they were (S05-H2 Open, S05-M3 Partial) and are NOT closed by this session. This is an out-of-order pickup, not the plan's next scheduled session — it does not supersede or complete S3-13's full 3-finding scope, and it does not advance NEXT SESSION past S3-10, which remains the plan's actual next unstarted session. Lane SRV ran: npm test 195/196 pass (server suite); the 1 failure is the same pre-existing identityVerify.test.js missing-native-module issue documented since S3-05 through S3-09, not a regression. Commit e776a1f (implementation + this session's tracker/log update together — see SESSION-S3-13.md's git-discipline correction note). See SESSION-S3-13.md for full detail.
              S3-09  PARTIAL — 3 of 5 plan-scoped findings addressed this session (S06-H1 and S06-I1 were already Fixed before this session, not re-touched). S06-M2 (_duressNonces unbounded growth) and S06-L1 (nonce expiry fails open) were both re-verified already-fixed from source — tracker was stale, not the code: firestore.indexes.json already declares a TTL policy on _duressNonces.expiresAt, /requestLockNonce already prunes any prior nonce for the uid before issuing a new one, and /duress-lock's transaction already validated expiry shape and failed closed. S06-L1's inline check was extracted to new pure resolveNonceExpiry()/isNonceUsable() in server/lib/pure.js (wired into the transaction) with 9 new regression tests, since the fix previously had zero test coverage. S06-M3 (raw uids logged on duress endpoints) was fully closed — the two remaining un-redacted lines (/admin/api/duress/enroll and /admin/api/duress/revoke) now use uidTag(uid); grep for the old pattern returns zero hits anywhere in index.js. As a side effect this also advanced (but did not close) S05-M1, which remains Partial: raw adminIp writes to the no-TTL adminAuditLog collection (4 call sites) and 2 requestId log lines are carried forward explicitly, out of this session's scope. Lane SRV ran: npm test 194/195 pass (server suite); the 1 failure is the same pre-existing identityVerify.test.js missing-native-module issue documented since S3-05 through S3-08, not a regression. See SESSION-S3-09.md for full detail.
              S3-08  PARTIAL — 3 of 4 plan-scoped findings closed. S04-M2 (TURN credential hardening — new pure clampTurnCredentialTtlSeconds() clamps TTL to [60s, 3600s] regardless of env misconfig, new checkTurnDailyCap() layers a 24h/100-per-account cap on top of the existing per-minute limiter, outbound Cloudflare fetch now has an 8s AbortController timeout, iceServers shape validated before responding 200) fixed, commit 8f6b206. S04-L2/S06-L2 (/duress-lock unauthenticated/unrestricted) re-verified fixed from source, not re-implemented — the endpoint already required a single-use uid-bound nonce (issued only post-auth by /requestLockNonce) and was already gated by checkIpRateLimit() as its first line; traced the only legitimate caller (Android AccountLockWorker) and confirmed the gate does not block it. S04-I3 (preview provenance) partial by design — new pure previewDomainFromUrl() labels /linkPreview's success response with the final validated redirect hop instead of the originally-submitted host, closing the phishing-labeling gap; the paired failure-indistinguishability half of the finding needs a coordinated Android change and stays open. S04-I1 (/status and / unauthenticated, publish counters) was in the plan's S3-08 scope but NOT attempted this session — remains Open, carried forward explicitly rather than folded into "all remaining open." Lane SRV ran: npm test 185/186 pass (server suite); the 1 failure is the same pre-existing identityVerify.test.js missing-native-module issue documented since S3-05/06/07, not a regression. See SESSION-S3-08.md for full detail, including which parts were reconciliation of a prior sub-session's uncommitted narrative vs. newly-run verification.
              S3-06  FIXED — all 4 findings in scope (S02-H1, S02-M1, S02-L1/S07-H1, S02-L2). Only S02-H1 (migrateUid field allow-list) and S02-L2 (createChat display-name bound) needed new code — new server/lib/profileSanitize.js + 9 unit tests, wired into server/index.js (commit 269d165). S02-M1 (pre-auth cooldown) and S02-L1/S07-H1 (mint hash fail-open) were re-verified from source and found already fixed in a pre-existing commit (5c2cd73, predates this tracker) — no code change, tracker corrected in place. Lane SRV actually ran: npm test 146/147 pass (1 pre-existing unrelated failure, see SESSION-S3-06.md §3). See SESSION-S3-06.md for full detail.
              S3-05  PARTIAL — all 6 findings (S01-M1, S01-M2, S01-M3, S01-M4, S01-L1 finish, S01-L2), RULES lane verify BLOCKED here → promoted in S3-15b. Rule fixes landed earlier in commit 3070b0b (undocumented at the time — this session's first task was reconciling that gap, not new rule code); this session corrected/extended the regression tests against those same rules (commit ec8a919) and wrote the tracker/session-log/chain-state documentation that had lagged behind 3070b0b. See SESSION-S3-05.md for full detail.
              S3-04  PARTIAL (S01-H1, S01-H2, S01-H3 — cross-user write protection, lane RULES, verify BLOCKED → S3-15b) — commit 812813d (impl) + fd6ce2c (tracker update). [Chain state was not advanced past S3-03 when this landed; corrected as part of S3-05's reconciliation pass above — no code change made to 812813d itself.]
              S3-03  COMPLETE (5 fixed + SC-03 partial, CI-lane verified from source): SC-06 JitPack scoped to com.github.* via includeGroupByRegex; SC-07 validate-gradle-wrapper job gating lint; SC-08 all GitHub Actions SHA-pinned (zero @vN refs remain); SC-09 dependabot.yml + security-scan.yml (CodeQL+gitleaks+SBOM); SC-10 npm ci + firebase-tools@15.26.0 in both firestore workflows. SC-03 stays PARTIAL — verification-metadata.xml scaffold committed, component-hash population BLOCKED (needs Gradle+Android SDK+network). Commits a3106df (impl) + 289c102 (tracker + log), merged PR #69 (62b5f5d).
              S3-02  COMPLETE (all 3 findings fixed & CI-lane verified): SC-05 scoped tag-clear (commit 8508746); SC-01 reproducible/hash-gated libsignal JAR + verify-libsignal-jar CI gate; SC-04 SHA256SUMS + signing-cert digest + attest-build-provenance on release (commit dcf85c5). Session log + chain state reconciled to committed source.
              S3-01  (APK/CI secrets: S08-C1, SC-02, S08-H1, S03-L1 code-fixed & verified; SC-12 re-checked live, still open/operator) — commit 60c8cde
BLOCKED GATES PENDING: S3-15b (RULES emulator — now covers S3-04 and S3-05's combined 9 rule findings), S3-19b (Android build) — need operator toolchains
OPERATOR RUNBOOK (S3-01): revoke leaked GCP SA key; rotate WORKER_SECRET + baked B2 creds; enable branch protection on main (SC-12). (S3-03) populate + enforce Gradle dependency-verification hashes (`./gradlew --write-verification-metadata sha256 help`, then flip `<verify-signatures>true`) once Android SDK + network are available.
```

If `NEXT SESSION` above is `S3-20 complete`, do **not** start coding — go to the sign-off gate at the
bottom of the plan and verify operator items instead.

---

## Do this, in order

1. **Read the rules of engagement.** Open [`SESSION_PROTOCOL.md`](./SESSION_PROTOCOL.md) and read it
   in full. It is binding: source beats tracker, ≤4 tasks/session, never fabricate a PASS, never make
   `/mintToken` auth fields optional.
2. **Open the plan.** In [`ROUND3_REMEDIATION_PLAN.md`](./ROUND3_REMEDIATION_PLAN.md), find the
   session named in `NEXT SESSION` above. That session's finding IDs, code targets, verification lane,
   and exit criteria are your entire scope. **Do not pull work forward from a later session.**
3. **Recover state (SESSION_PROTOCOL §3 + §7).** Run:
   - `git status --short` and `git log -3 --oneline --stat`
   - For each finding ID in this session, read its row in [`../BUG_TRACKER.md`](../BUG_TRACKER.md) and
     run the smallest check that would falsify its current status (`grep` the vulnerable pattern; if a
     commit is cited, `git show --stat <hash>`). If source already satisfies it, record that and skip.
4. **Implement the smallest fix** for this session's findings only. Keep existing guards; add beside
   them. Honor the standing invariants in the plan (especially: `nonce`/`signatureHex` stay mandatory).
5. **Verify in this session's lane** (the plan names it):
   - `SRV` → `cd server && npm test` (must stay green; add cases for the findings).
   - `WORKER` → `node --check` + worker unit tests.
   - `RULES` → write emulator tests now, but the run is **BLOCKED** here → land as
     `partial — RULES verification BLOCKED`; promotion happens in **S3-15b**.
   - `AND` → write/adjust code + tests now, run is **BLOCKED** here → land as
     `partial — AND verification BLOCKED`; promotion happens in **S3-19b**.
   - `CI` → YAML lint + `git`/`gh` inspection + hash recompute; actual CI run is operator.
   - **If a toolchain is missing, mark BLOCKED and fall back to source review. Never invent a PASS.**
6. **Document + checkpoint** (mandatory before stopping):
   - Update each finding's status in [`../BUG_TRACKER.md`](../BUG_TRACKER.md) with real
     evidence (a command you ran this session, or a commit hash from `git log -1 --format=%H` **after**
     committing). Only write `fixed` if the lane's verification actually ran and passed.
   - Write `sessions/SESSION-<n>.md` with the end-of-session record block (format in
     `SESSION_PROTOCOL.md §7`).
   - Commit; confirm `git status` is clean.
   - **Update the Chain state block above**: set `NEXT SESSION` to the following session, set
     `LAST DONE` to the one you just finished.
7. **Stop.** One session = one scheduled batch. Do not start the next session because budget remains.

---

## Operator-only items (do not fake; only re-verify)

These cannot be completed by a session in this environment — revoke the leaked GCP service-account
key, rotate `WORKER_SECRET`/B2/admin creds, enable `SC-12` branch protection, Firestore TTL, App
Check enable, SBOM, and the Android build+release. A session may **re-check** whether an operator has
done one (e.g. `gh api repos/kumarclaude4-hash/DuoFatass/branches/main/protection`) and update the
disposition from live evidence — but must never report an operator action as done without that
evidence. Tracked in the plan's sign-off gate and `SESSION_PROTOCOL.md §6`.

---

## Reading order summary

`START_HERE.md` (this file, for `NEXT SESSION`) → `SESSION_PROTOCOL.md` (rules) →
`ROUND3_REMEDIATION_PLAN.md` (the session's scope) → `../BUG_TRACKER.md` (per-row truth) →
previous `sessions/SESSION-*.md` (inherited evidence). Then work.
