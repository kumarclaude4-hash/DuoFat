<?xml version="1.0" encoding="utf-8"?><!--
  One YouTube search result row in the Watch Together picker.
  Thumbnail is 16:9 to match YouTube's mqdefault so no crop surprise; the whole row is the
  touch target (56dp+ tall) rather than just the title.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:minHeight="72dp"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <ImageView
        android:id="@+id/ivResultThumb"
        android:layout_width="112dp"
        android:layout_height="63dp"
        android:scaleType="centerCrop"
        android:background="@color/ds_surface_2"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvResultTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="2"
            android:ellipsize="end"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:lineSpacingMultiplier="1.2" />

        <TextView
            android:id="@+id/tvResultChannel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/ds_text_tertiary"
            android:textSize="12sp" />
    </LinearLayout>

</LinearLayout>
